# Next.js
NEXT_MONGODB_URI=
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_GEMINI_API_KEY=

# Local YOLO inference (optional). When true, /api/fridge/detect uses the Python service.
USE_LOCAL_MODEL=false
LOCAL_MODEL_URL=http://localhost:8000

# (Python /inference only) Path to weights — relative to inference/ or absolute.
# MODEL_PATH=models/best.pt
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
